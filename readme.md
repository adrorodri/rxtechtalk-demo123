# Hello!

To run this demos, first install node depencies

```npm i```

then start the demo server:

```npm run server```

Then execute in another terminal, any demo with:

```npm start demo1```

```npm start demo2```

```npm start demo3```